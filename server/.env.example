PORT=4000
JWT_SECRET=change-this-to-a-long-random-string

# --- Social login (optional) ---
# Create these in Google Cloud Console (OAuth consent screen + OAuth client ID,
# type "Web application", with your site's URL under Authorized JavaScript origins).
GOOGLE_CLIENT_ID=64676272437-bjl4nnqekk7og2u6l3ki0gab7m1eqc4i.apps.googleusercontent.com

# Facebook login only needs the App ID on the frontend (js/config.js).
# Nothing Facebook-specific is required back here unless you later add
# server-side Graph API calls beyond verifying the login token.
